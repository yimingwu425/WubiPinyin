<Page
    x:Class="WubiPinyinSettings.DictionaryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    Loaded="Page_Loaded">
  <Grid Padding="32,20,32,36" ColumnSpacing="24">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="2*" MinWidth="360" />
      <ColumnDefinition Width="*" MinWidth="280" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.ColumnSpan="2" Style="{StaticResource SettingsPageTitleStyle}" Text="用户词库" />
    <controls:InfoBar x:Name="StatusBar" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,16,0,16" IsClosable="False" IsOpen="False" />

    <Grid Grid.Row="2" ColumnSpacing="24">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <TextBox x:Name="SearchBox" PlaceholderText="搜索词条或编码" TextChanged="SearchBox_TextChanged" />
      <Border Grid.Row="1" Margin="0,12,0,0" Style="{StaticResource SettingsGroupBorderStyle}" Padding="0">
        <ListView x:Name="EntriesList" SelectionChanged="EntriesList_SelectionChanged" SelectionMode="Single" />
      </Border>
    </Grid>

    <ScrollViewer Grid.Row="2" Grid.Column="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
    <StackPanel Spacing="8">
      <TextBlock Style="{StaticResource SettingsSectionTitleStyle}" Text="词条" />
      <Border Style="{StaticResource SettingsGroupBorderStyle}">
        <StackPanel Spacing="12">
          <TextBox x:Name="EntryTextBox" Header="文字" />
          <ComboBox x:Name="SchemeBox" Header="方案" SelectedIndex="0">
            <ComboBoxItem Content="五笔" Tag="wubi" />
            <ComboBoxItem Content="拼音" Tag="pinyin" />
          </ComboBox>
          <TextBox x:Name="CodeBox" Header="编码" />
          <NumberBox x:Name="WeightBox" Header="权重" Minimum="1" Maximum="100000" SmallChange="100" SpinButtonPlacementMode="Inline" Value="1000" />
          <ToggleSwitch x:Name="EnabledToggle" Header="启用" IsOn="True" />
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button Content="新建" Click="NewEntry_Click" />
            <Button Content="保存" Click="SaveEntry_Click" Style="{StaticResource AccentButtonStyle}" />
            <Button x:Name="DeleteButton" Content="删除" Click="DeleteEntry_Click" IsEnabled="False" />
          </StackPanel>
        </StackPanel>
      </Border>
    </StackPanel>
    </ScrollViewer>
  </Grid>
</Page>
